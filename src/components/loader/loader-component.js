import styled, { css } from 'styled-components';

const loaderWrapStyle = css`
  padding: 50px 0;
  display: flex;
  justify-content: center;
`;

export const Loader = styled.div`
  ${loaderWrapStyle}
`;
